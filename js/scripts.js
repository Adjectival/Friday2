$(document).ready(function() {
  $("form#whereto").submit(function(event) {
    event.preventDefault();

    var howRemote = 0;
    var howLuxury = 0;
    var howNatural = 0;

    if () {

    } else if () {

    } else if () {

    } else if () {

    } else {

    }

    $('.output').show();

  });
});
